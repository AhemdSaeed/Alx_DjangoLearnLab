# LibraryProject

This is my first Django project for the ALX introduction to Django environment setup task.

## Steps to run
1. Install Django using `pip install django`
2. Run the server with `python manage.py runserver`
3. Open http://127.0.0.1:8000/ in your browser to see the welcome page.
